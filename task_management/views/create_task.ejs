<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Task Manager</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            background-color: #6ea7e0;
            
        }
        .container {
            max-width: 800px;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            
        }
        .navbar {
            margin-bottom: 20px;
        }
        .form-group input, .form-group textarea, .form-group select {
            border-radius: 5px;
        }
        .btn-secondary {
            background-color: #007bff;
            border-color: #007bff;
            font-weight: bold;
        }
        .btn-secondary:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <a class="navbar-brand" href="#">Task-Manager</a>
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="/task/create-task">Create Task</a></li>
            <li class="nav-item"><a class="nav-link" href="/task/all-task">All Task</a></li>
            <li class="nav-item"><a class="nav-link" href="#">Completed Task</a></li>
            <li class="nav-item"><a class="nav-link" href="#">Sign out</a></li>
        </ul>
    </nav>

    <div class="container mt-4">
        <h3 class="text-center mb-4">Create a New Task</h3>
        <form action="/task/create-task" method="post">
            <div class="form-group">
                <input name="title" type="text" placeholder="Enter task title" class="form-control" required />
            </div>
            <div class="form-group">
                <textarea name="description" class="form-control" placeholder="Write task description" required></textarea>
            </div>
            <div class="form-group">
                <select class="form-control" name="priorityId" required>
                    <option value="" disabled selected>Select task priority</option>
                    <% for(let taskPriority of taskPriorities) { %>
                        <option value="<%= taskPriority.id %>"><%= taskPriority.priority %></option>
                    <% } %>
                </select>
            </div>
            <div class="form-group text-center">
                <button class="btn btn-secondary btn-block" type="submit">Create Task</button>
            </div>
        </form>
    </div>
</body>
</html>
